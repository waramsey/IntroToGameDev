class Coordinates {
    // PlayerX is 0
    // PlayerY is 1
    // crystals left is 2
    // boss alive is 3 (1 is true, 0 is dead)
    // health is 4

    static coords = [0, 0, 3, 1, 100];
}

export default Coordinates;